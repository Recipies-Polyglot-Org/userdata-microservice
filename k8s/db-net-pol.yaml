apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: postgres-net-pol
  namespace: default
spec:
  podSelector:
    matchLabels:
      app: postgres
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: backend-userdata
    ports:
    - protocol: TCP
      port: 5432